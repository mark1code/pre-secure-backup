# pre-secure-backup

**Proxy Re-Encryption Secure Backup**

An implementation of a secure backup system using AES-GCM for file encryption and unidirectional proxy re-encryption (Umbral) for cryptographic access delegation.

